Online JOGL and jSurfer libs
============================

The libraries in this folder are digitally signed and can be used with Java WebStart. The "codebase" field in the .jnlp files must be changed to the location, where the folders are uploaded.

